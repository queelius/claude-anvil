---
description: "Fork the current research run into a named branch exploring a competing strategy"
---
Run the branch skill: fork the run at its current checkpoint into `.research/branches/<name>/` per $ARGUMENTS (branch name plus optional focus).
