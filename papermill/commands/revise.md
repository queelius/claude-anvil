---
description: "Apply fixes from a review report via the multi-agent reviser (fix-then-verify)"
---
Run the revise skill: locate the review report (or use the path in $ARGUMENTS), launch the reviser orchestrator, and update the state file.
